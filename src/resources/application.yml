server:
  port: 5000

telemetry:
  port: 4317
  host: localhost

kafka:
  client:
    clientId: todo-backend
    brokers: localhost:9092
  consumer:
    groupId: todo-consumer
    allowAutoTopicCreation: true
